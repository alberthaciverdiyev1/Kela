/**
 * İndeks səhifələri (students / lessons / quizzes / workspaces).
 * Silmə formaları (x-data="deleteForm(...)"), tema düyməsi və flash
 * bildirişləri üçün Alpine-i başladır. start() idempotentdir.
 */
import Alpine from 'alpinejs';

Alpine.start();
